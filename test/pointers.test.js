const expect = require('chai').expect;
const bind = require('./build/Release/napi_bind_test.node')

describe("pointers", () => {
  it("void *", () => {
    expect.fail()
  });

  it("struct *", () => {
    expect.fail()
  });
});